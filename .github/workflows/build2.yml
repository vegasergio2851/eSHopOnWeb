name: Build

on:
  push:
    branches:
      - main


jobs:
  build:
    name: Build
    runs-on: self-hosted
    permissions: read-all
    steps:
      - name: Build and analyze
        env:
         shell: powershell
        run: |
          C:\Users\"Sergio Vega"\.dotnet\tools\scanner\dotnet-sonarscanner begin /k:"vegasergio2851_eSHopOnWeb_AYuXne0WlV_fnToDXzVE" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.host.url="${{ secrets.SONAR_HOST_URL }}"
          dotnet build
          C:\Users\"Sergio Vega"\.dotnet\tools\scanner\dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
 
